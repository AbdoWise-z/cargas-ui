class ShopItem{
  double price = 0;           // price of item in L.E.
  String name = "No name";    // the name of the item in the shop
  int image = 0;              // the index of the image in the listed assets images
  int itemCode = -1;          // item code , in sever (or maybe serial)
}